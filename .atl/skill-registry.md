# Skill Registry — claude-subagent-statusline

> Auto-generated by `sdd-init`. Lists user-level skills relevant to this Claude Code plugin project (bash + JSON manifests).

## Project Conventions

No project-level convention files yet (no `CLAUDE.md`, `AGENTS.md`, etc. in repo root). Will inherit from user-level `~/.claude/CLAUDE.md`.

## User Skills (auto-resolved by code/task context)

| Skill | When it applies | Path |
|-------|-----------------|------|
| branch-pr | Creating PRs against this repo or downstream gentle-ai PR | `~/.claude/skills/branch-pr/SKILL.md` |
| issue-creation | Opening GitHub issues for v0.1 announcement or follow-up bugs | `~/.claude/skills/issue-creation/SKILL.md` |
| judgment-day | Optional dual-review of the plugin scripts before v0.1 tag | `~/.claude/skills/judgment-day/SKILL.md` |

## Compact Rules (injected into sub-agent prompts on demand)

### branch-pr
- Branch naming: `feat/<topic>` for features, `fix/<topic>` for bugs
- PR title: conventional commits style
- PR body: include "Closes #N" for the linked issue
- Never `--no-verify`, never amend after push

### issue-creation
- Use the project's issue templates if present (none here yet — generic format ok)
- Title: imperative + scope (e.g., `feat: add macOS tap`)
- Body: Problem → Proposal → Acceptance criteria
- Label `status:needs-review` if applicable

### judgment-day
- Trigger only on user request or before publishing a tagged release
- Two parallel blind reviewers, then synthesize
- Re-judge after fixes, escalate after 2 iterations
