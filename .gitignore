.DS_Store
*.swp
*.swo
*~
.idea/
.vscode/
node_modules/
dist/
*.log
/tmp/

# Local agent tooling. Both are machine-specific and regenerated by local hooks,
# so they differ per contributor and only ever produce commit noise.
.atl/
.claude/

# No lockfile to keep: package.json declares zero dependencies, so npm only ever
# writes a lockfile describing this package itself.
package-lock.json
